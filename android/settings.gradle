pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }

    // plugins {
    //     id "dev.flutter.flutter-gradle-plugin" version "1.0.0"
    // }
}

def pluginDependencies = new File(rootDir, ".flutter_plugin_dependencies")
if (pluginDependencies.exists()) {
    includeBuild(".flutter_plugin_dependencies")
}

include(":app")